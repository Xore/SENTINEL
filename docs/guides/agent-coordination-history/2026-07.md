# Agent Coordination History — July 2026

## Ledger Bootstrap

- **Commit:** `0fb9571`
- Added the Sonnet 5 implementation guide and shared coordination ledger.
- Established Codex ownership of architecture/contracts/security/integration and
  Sonnet ownership of assigned bounded implementation slices.

## C0-01 — Repository Audit and Requirements Traceability

- **Owner:** CODEX
- **Status:** DONE
- **Scope:** `docs/architecture/REQUIREMENTS-TRACEABILITY.md`
- **Commits:** `be6c04b`, handoff/transition `0631555`
- **Result:** Audited every extended-architecture feature. The repository was an
  early collector plus dev-hub slice; production ingest/API/analyse/frontend and
  almost all extended features were not implemented.
- **Decision:** Documentation/examples do not count as implementation.
- **Review:** Accepted. C0-02 unblocked.

## C0-02 — Architecture Decisions and Canonical Contracts

- **Owner:** CODEX
- **Status:** DONE
- **Commits:** `569c19e`, handoff `5a588a2`
- **Artifacts:** ADR 0001–0010 and `docs/contracts/*`.
- **Decisions:** canonical service layout; site OTLP ingest path; `sentinel_`
  metric prefix; certificate-bound identity; idempotent event envelope;
  application-level federation; federated-update privacy controls; bounded
  passive Modbus metadata; constrained signed updater; production HA/version
  pins; measured scale envelopes; forward-only migrations.
- **Validation:** Markdown diff checks and JSON Schema parsing passed; remote
  read-back verified.
- **Review:** Accepted. C1-01 and S1-01 contract prerequisite satisfied.

## S0-01 — Collector Quality Audit

- **Owner:** SONNET5
- **Status:** DONE
- **Claim commit:** `85e7fc5`
- **Handoff commit:** `1d80746`
- **Scope:** Read-only source audit; coordination ledger only.
- **Environment:** clean Python 3.12.3 virtual environment.
- **Results:**
  - Ruff 0.16.0: passed.
  - mypy 1.20.2: no issues in 35 files.
  - Pylint 3.3.7: 10.00/10.
  - pytest 9.1.1: 125 passed, 1 skipped in 1.07 seconds.
  - `pip check`: no broken requirements.
- **Confirmed gaps:**
  1. no automated Phase 1 untrusted/expired/mismatched/reused-token/outage tests;
  2. collector Pi resource gates unmeasured;
  3. OTel dotted resource attributes previously disappeared during remote-write,
     validating the underscored label contract;
  4. shared `aiohttp.ClientSession` lacked shutdown closure;
  5. no production Docker/PyInstaller packaging.
- **Review commit:** `671d2d2`
- **Review:** Accepted; findings assigned into S1-01 and C1-01.

## C1-01 — Early Implementation Checkpoint

- **Owner:** CODEX
- **Status at archive creation:** IN_PROGRESS
- **Commit:** `62e8aef`
- Added Go certificate identity parsing/matching, tests, and initial PostgreSQL
  migration for sites, collectors, enrollment tokens, idempotent durable events,
  and federation outbox.
- `go test ./...` and `go vet ./...` passed.
- Remains active in the main ledger.

## S1-01 — Collector Contract and Lifecycle Hardening

- **Owner:** SONNET5
- **Status:** DONE
- **Claim commit:** `3ed619d`
- **Implementation/handoff commit:** `cc988cf`
- **Review date:** 2026-07-26
- **Result:**
  - enforced ADR 0009 DNS-label collector/site identity;
  - emitted canonical `sentinel_collector_heartbeat_total` plus the temporary
    compatibility alias;
  - set OTel `service.name=sentinel-collector` and development
    `service.version`;
  - added `BaseCheck.aclose()`, resilient shutdown closure, and shared HTTP
    session closure;
  - added 29 tests without removing or weakening prior tests.
- **Sonnet verification:** Ruff and mypy passed, Pylint 10/10, pytest
  155 passed/1 skipped on Linux.
- **Codex review:**
  - GitHub collector, Pylint, and CodeQL runs at `cc988cf` passed.
  - Independent Windows Ruff/mypy passed. Windows revealed pre-existing
    POSIX-mode and Python 3.14 Pylint portability assumptions, assigned to
    S1-02 rather than treated as S1-01 regressions.
  - `2.0.0-dev` is accepted until release automation supplies a release version.
  - Live `service.name` → `service_name` mapping remains a C1-01 integration
    test; the unit test is specification-level only.
- **Review outcome:** Accepted.

## C1-01 — Hub, Migrations, PKI, and Ingest Foundation

- **Owner:** CODEX
- **Status:** DONE
- **Implementation commits:** `62e8aef`, `96a21b6`, `73ae0d9`, `8fbe62f`,
  `a42b81f`, `4b3c36d`, `5013b7d`, `15f8eb1`, `45f8e65`
- **Result:**
  - TLS 1.3 mTLS OTLP ingest derives exactly one canonical SPIFFE collector
    identity and rejects payload identity overrides;
  - PostgreSQL authorization rejects unknown, disabled, and certificate-
    mismatched collectors and updates `last_seen`;
  - VictoriaMetrics forwarding is bounded and preserves the required resource
    labels;
  - the advisory-locked forward-only runner applies embedded migrations
    transactionally, records SHA-256, and rejects changed, gapped, or future
    histories;
  - HTTPS enrollment validates the CSR, atomically consumes a token digest,
    signs a client-auth leaf, and persists certificate serial/expiry;
  - the dev hub now exercises production Go migration, enrollment, and ingest
    instead of the former raw SQL and OTLP/PKI stubs.
- **Verification:**
  - Windows Go race/vet/build passed with Go 1.26.3 and MSYS2 UCRT64 GCC.
  - GitHub runs `30197581879`, `30197933702`, and `30198152790` passed Go,
    PostgreSQL, container-build, and Compose gates.
  - Ubuntu 24.04 host `.33` passed exact-commit race/vet/build and PostgreSQL
    integration. PostgreSQL 16.14, Docker 29.1.3, and Compose 2.40.3 were
    installed for reproducible Linux testing.
  - Phase 1 E2E at `45f8e65`: collector `site-a/dev-node-1` enrolled over
    HTTPS, authenticated to Go ingest via mTLS, emitted
    `sentinel_collector_heartbeat_total`, and VictoriaMetrics returned the
    required `site_id` and `collector_id` labels. PostgreSQL recorded the
    issued serial and `last_seen=true`.
  - The disposable `sentinelcodex` containers, network, volumes, collector
    files, CA copy, and checkout were removed after the gate. Host PostgreSQL
    and Docker services remain installed and active; `.32` was never accessed.
- **Review outcome:** Accepted; Phase 1 backend integration foundation complete.

## C1-03 — Automated Collector-to-Storage Integration Smoke

- **Owner:** CODEX
- **Status:** DONE
- **Claim commit:** `567ffb4`
- **Implementation commits:** `ba1dced`, `478c66b`, `e0e6ebe`
- **Scope:** `.github/workflows/integration-test.yml`.
- **Result:** Added a bounded GitHub-hosted Phase 1 vertical-slice test that
  starts the disposable production-path hub, waits for Go ingest and
  VictoriaMetrics, copies the throwaway enrollment CA, runs the real Python
  collector, and requires the canonical heartbeat series with exact
  `site-a/dev-node-1` identity labels.
- **Failure diagnostics:** collector output plus full Compose state/logs are
  printed and volumes are removed unconditionally. The workflow uses only the
  deterministic disposable development token and CA.
- **Verification:** GitHub Actions run `30198940453` passed all steps in 2m38s,
  including migrations, seed, HTTPS enrollment, TLS 1.3 mTLS OTLP ingest,
  VictoriaMetrics query assertion, diagnostics, and cleanup.
- **Review outcome:** Accepted and archived at 2026-07-26T10:51:40Z.

## C1-04 — Authenticated Site API and Collector Status

- **Owner:** CODEX
- **Status:** DONE
- **Claim commit:** `b24ddc4`
- **Implementation commits:** `4480d91`, migration-test fix `496b5fa`,
  dependency security fix `94db19d`, integration-filter fix `a7a6500`
- **Coordination refinement:** `79dfbb2`
- **Dependency automation read-back:** Dependabot commit `2beafbc` was pulled
  and inspected before continuing.
- **Result:**
  - created the canonical `backend/api/` Go/Gin service with bounded
    health/readiness, graceful shutdown, PostgreSQL pooling, a non-root
    container, and `GET /api/v1/collectors`;
  - added strict HS256 bearer validation with a fixed algorithm, issuer,
    audience, `iat`/`nbf`/`exp`, bounded site scope, and a minimum 256-bit
    secret;
  - enforced current database user status, exact current role,
    `token_not_before`, and the intersection of JWT and database site access;
  - added forward-only migration `000002_api_users.sql`, deterministic
    development seed, versioned `docs/contracts/API.md`, and Compose wiring;
  - returned stable fleet state including `last_seen`, silence, enrollment, and
    certificate-expiry fields without leaking unauthorized site existence;
  - expanded backend and Phase 1 CI so API-only changes run unit/race/vet/build,
    container, migration, PostgreSQL authorization, and live vertical-slice
    gates.
- **Verification:**
  - local Windows non-race tests, vet, and build passed; Windows race linking
    was blocked by the local GCC/Smart App Control path;
  - Ubuntu `.33` at exact implementation commit passed Go race/vet/build, API
    container and Compose health, PostgreSQL integration, authorized
    `site-a/dev-node-1` output, and empty unauthorized `site-b` output;
  - the disposable `.33` containers, network, volumes, and checkout were
    removed after testing; reusable Docker build cache/images were retained;
  - GitHub backend run `30199744668` passed API/ingest format, vet, race, build,
    containers, Compose, migrations, and PostgreSQL integration;
  - GitHub Phase 1 run `30199788361` passed real enrollment, mTLS heartbeat,
    VictoriaMetrics identity, authenticated `last_seen`, unauthorized-site
    isolation, diagnostics, and cleanup in 3m7s;
  - Dependabot alert 79 for `golang.org/x/net < 0.55.0` is fixed by the pinned
    patched version.
- **Review outcome:** Accepted and archived at 2026-07-26T11:19:00Z.
