# Port health checks run every PROBE_MONITOR_PORT_INTERVAL seconds (default 60).
# Columns: name,host,port,proto,send,expect
#   proto  - tcp (default) or udp
#   send   - optional bytes to send after connect; supports \r\n \t \xNN escapes.
#            Leave blank to use the well-known default for the port.
#   expect - optional regex matched against the response. Blank = use the
#            well-known default (e.g. HTTP/ for 80/443, ^SSH- for 22).
#
# Well-known ports probe and expect automatically from a bare line, e.g.:
#   web,10.0.0.10,80,,,          -> GET / , expects an HTTP status line
#   secure-web,10.0.0.10,443,,,  -> TLS + GET , expects an HTTP status line
#   ssh,10.0.0.10,22,,,          -> expects ^SSH- banner
#   dns-tcp,10.0.0.53,53,,,      -> real DNS query, expects a valid reply
#   redis,10.0.0.20,6379,,,      -> PING , expects +PONG
#
# OT/control ports (102 S7, 502 Modbus, 4840 OPC UA, PROFINET, EtherNet/IP,
# DNP3, BACnet) are CONNECT-ONLY: the probe never sends application payloads,
# and a custom send/expect is ignored for them.
#
# Custom port with your own request and expected response:
#   healthcheck,10.0.0.30,9000,tcp,GET /healthz HTTP/1.0\r\nHost: h\r\n\r\n,^HTTP/1.[01] 200
#
# name,host,port,proto,send,expect
web-external,1.1.1.1,443,,,
dns1-tcp,192.168.42.250,53,,,
dns2-tcp,192.168.42.252,53,,,
# s7-plc,192.0.2.10,102,,,       # connect-only (OT)
# opcua-server,192.0.2.20,4840,,,# connect-only (OT)
# modbus-plc,192.0.2.11,502,,,   # connect-only (OT)
